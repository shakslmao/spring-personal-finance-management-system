spring:
    security:
        oauth2:
            resourceserver:
                jwt:
                    issuer-uri: http://localhost:9098/realms/finance-microservice

    cloud:
        gateway:
            discovery:
                locator:
                    enabled: true
            routes:

server:
    port: 8222
