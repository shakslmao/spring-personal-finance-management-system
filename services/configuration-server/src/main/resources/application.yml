spring:
  profiles:
    active: native
  application:
    name: configuration-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations
server:
  port: 8888 # Configuration Server Port.